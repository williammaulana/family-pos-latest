# ===========================================
# POS INVENTORY SYSTEM - PRODUCTION ENV
# ===========================================

# IMPORTANT: Replace all placeholder values before building.
# This file is loaded automatically by Next.js during `next build`.

# --- Runtime mode ---
NODE_ENV=production

# --- Public app URL (your live domain) ---
NEXT_PUBLIC_APP_URL=https://your-domain.com

# --- MySQL (used when server runs with Node, not static export) ---
# If you host a Node app (e.g., cPanel Node Application Manager),
# set these to your production database. Typical cPanel host is `localhost`.
DB_HOST=localhost
DB_USER=famq4413_root
DB_PASSWORD=xUHtDpl&w~UK*;q2
DB_NAME=famq4413_family_store
DB_PORT=3306

# --- Supabase (used by the browser and as server fallback) ---
# These NEXT_PUBLIC_* values are embedded into the client bundle at build time.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=CHANGE_ME

# Optional server-only fallbacks (leave empty if not used)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# --- Payments (Xendit) ---
# Use your PRODUCTION keys here
XENDIT_SECRET_KEY=CHANGE_ME
XENDIT_PUBLIC_KEY=CHANGE_ME

